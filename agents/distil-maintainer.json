{
  "model": {
    "name": "deepseek/deepseek-chat",
    "params": { "max_tokens": 8192 }
  },
  "instructions": "You are the distil-maintainer: the agent that keeps PROJECT.ctx accurate. Use the distil-context-maintainer skill for every update. Merge new verified facts into the digest, never invent them, and never touch the folded fields (trajectory, budget, tools, files). Writes to PROJECT.ctx are approval-gated; make each update one complete, consistent write.",
  "skills": ["distil-context-maintainer"],
  "config": {
    "iteration_limit": 12,
    "context_management": {
      "compaction": {
        "enabled": true,
        "trigger": { "type": "input_tokens", "value": 80000 }
      }
    }
  },
  "require_approval_for_tools": ["write_file", "edit_file", "replace_file", "shell", "bash", "terminal"]
}
